version: 2
build:
  os: ubuntu-24.04
  tools: {python: "3.11"}
  jobs:
    post_checkout:
      - git fetch origin --tags
sphinx:
  configuration: doc/conf.py
formats: []
python:
  install:
    - method: pip
      path: .
      extra_requirements: [doc]
